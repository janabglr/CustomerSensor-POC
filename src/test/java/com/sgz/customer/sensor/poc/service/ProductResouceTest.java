package com.sgz.customer.sensor.poc.service;


import static org.junit.Assert.assertEquals;

/**
 * Created by janardhan.nellibanda on 9/26/2016.
 */
public class ProductResouceTest extends ProductRESTTestBase{
/*

    private ProductAccessService productAccessService;
    private CustomerSensorRequest productRequest;
    @Before
    public void setUpServices() {

        productRequest = TestUtil.getProductRequest();
        productAccessService = new ProductAccessService();
    }


    @Override
    protected AppDescriptor configure() {

        MockableContext.addMock("productAccessService", productAccessService);

        //configureBase();
        return super.configure();
    }

    @Test
    public void getProductsTest() {
        ClientResponse clientResponse = resource().path("product/getAllProduct")
                .accept(MediaType.APPLICATION_XML).get(ClientResponse.class);
        assertEquals("", Response.Status.OK.getStatusCode(), clientResponse.getStatus());
        AddCustomerSensorResponse productResponse = clientResponse.getEntity(AddCustomerSensorResponse.class);

    }

    @Test
    public void addProductTest() {
        ClientResponse clientResponse = resource().path("product/addProduct").accept(MediaType.APPLICATION_XML).post(ClientResponse.class, productRequest);
        assertEquals("", Response.Status.OK.getStatusCode(), clientResponse.getStatus());
        AddCustomerSensorResponse productResponse = clientResponse.getEntity(AddCustomerSensorResponse.class);
      */
/*  Assert.assertTrue(productResponse.getProducts() != null);
        Assert.assertTrue(productResponse.getProducts().size() > 0);*//*

    }
*/

}
